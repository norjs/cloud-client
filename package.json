{
  "name": "@norjs/cloud-client",
  "version": "1.0.11",
  "description": "Client for NorJS's cloud framework",
  "main": "dist/index.js",
  "scripts": {
    "compile-watch": "babel --watch --presets es2015 -d dist/ src/",
    "compile": "babel --presets es2015 -d dist/ src/",
    "prepublish": "npm run compile",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/sendanor/cloud-client.git"
  },
  "author": "Jaakko-Heikki Heusala <jheusala@iki.fi>",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1"
  },
  "dependencies": {
    "lodash": "^4.17.11",
    "nor-debug": "^1.0.1",
    "nor-errors": "^1.0.0",
    "nor-is": "^1.0.1",
    "q": "^1.5.1"
  }
}
